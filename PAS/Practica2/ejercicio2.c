/*ejercicio2.c:
Cuando la comunicación entre procesos es simple y ambos extremos de la comunicación han
sido generados con un fork() o pertenecen al mismo proceso, se pueden utilizar tuberías
anónimas (pipes).

Las tuberías son unidireccionales, por lo que para hacer una comunicación full-duplex
se necesitarían dos tuberías. Implemente un programa en C que cree un proceso hijo usando
la primitiva fork(), de forma que el padre y el hijo se comunicarán mediante dos tuberías.
*/


int main(int argc, char const *argv[]) {
  
  return 0;
}
